

object Proj6_SparkSQL {
  //vid 4 details
  //sparkSQL(1.x) --> catalyst optimizer
  //sparkSession (2.x) -> tungsten optimizer
  
  
  //spark-shell --driver-memory 1G --dirver-class-path mysql-connector-java5.1.bin.jar
  //scala>  now we will get sql context as sqlContext
  // val hc=new org.pache.spark.spark.sql.hive.HiveContext(sc)
  //hc.sql("show databases")
  //hc.sql("create table emp(eid ...))
  
}